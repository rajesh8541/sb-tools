import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { SbHeadingComponent } from './sb-heading.component';

@NgModule({
  declarations: [SbHeadingComponent],
  imports: [CommonModule],
  exports: [SbHeadingComponent],
})
export class SbHeadingModule {}
